server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/items-assigner?useSSL=false&allowPublicKeyRetrieval=true&useAffectedRows=true
    username: root
    password: Aa1341512355
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      auto-commit: true
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 30000
      pool-name: MyHikariCP
  application:
    name: order-service
  data:
    redis:
      host: localhost
      port: 6379
      database: 3
      lettuce:
        pool:
          max-active: 20
          max-idle: 10
          min-idle: 5
          max-wait: 2000ms
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
    sentinel:
      transport:
        dashboard: localhost:8090
mybatis-plus:
  type-aliases-package: com.wddyxd.orderservice.pojo
#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:10086/eureka/
token:
  signKey: 123456
  expiration: 86400000
logging:
  config: classpath:logback-dev.xml
feign:
  sentinel:
    enabled: true